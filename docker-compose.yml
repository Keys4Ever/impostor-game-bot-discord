services:
  bot:
    build: .
    container_name: impostor-bot
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: file:/app/data/prod.db
    volumes:
      - ./data:/app/data
